@layer components {
   .header {
      @apply absolute top-0 inset-x-0;
      @apply w-full flex items-center justify-evenly font-mono text-sm text-yellow bg-purple opacity-75 py-0.5;
      @apply tablet:relative;
   }
}
